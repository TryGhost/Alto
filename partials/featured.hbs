{{#get "posts" filter="featured:true" limit="10" as |featured|}}
  {{#if featured}}
    <section class="featured-posts carousel owl">
      {{#foreach featured}}
        <article class="{{post_class}}{{#unless feature_image}} no-image{{/unless}}">
          {{> post-media static_image=true small_image=true same_height=true ratio="square"}}
          {{> post-header}}
        </article>
      {{/foreach}}
    </section>
  {{/if}}
{{/get}}