<article class="{{post_class}}{{#unless feature_image}} no-image{{/unless}}">
  {{> post-media}}
  {{> post-header}}
  {{#if excerpt}}
    <div class="post-excerpt">
      {{excerpt words="33"}}
    </div>
  {{/if}}
  {{> post-footer}}
</article>