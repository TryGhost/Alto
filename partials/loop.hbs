<article class="{{post_class}}{{#unless feature_image}} no-image{{/unless}}">
  {{> post-media same_height=true ratio="horizontal"}}
  {{> post-header big_title=true}}
  {{#if excerpt}}
    <div class="post-excerpt">
      {{excerpt words="33"}}
    </div>
  {{/if}}
  {{> post-footer}}
</article>